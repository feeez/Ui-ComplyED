import RegisterForm from '@/components/Register/RegisterForm'

export const metadata = {
  title: 'Create Account — ComplyED',
  description: 'Create your ComplyED account',
}

export default function RegisterPage() {
  return <RegisterForm />
}
