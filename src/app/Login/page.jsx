import LoginForm from '@/components/Login/LoginForm'

export const metadata = {
  title: 'Sign In — ComplyED',
  description: 'Sign in to your ComplyED account',
}

export default function LoginPage() {
  return <LoginForm />
}
